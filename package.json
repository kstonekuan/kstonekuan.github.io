{
	"name": "kingston-portfolio",
	"version": "0.1.0",
	"private": true,
	"devDependencies": {
		"tailwindcss": "^3.4.19"
	},
	"scripts": {
		"build:css": "tailwindcss -i src/styles/globals.css -o .trunk-staging/output.css --minify",
		"dev": "trunk serve",
		"build": "trunk build --release"
	}
}
